<ul class="tags_ul">
    {{#each leagues}}
    <li class="tags_li"><img src="{{this.imgPath}}" alt="{{this.name}}" class="tags_img"><input type="checkbox"
            name="{{this.name}}" id="{{this.name}}"></li>
    {{/each}}
</ul>